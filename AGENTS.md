# 26E_vision 项目专用提示词

## 项目范围

- 本项目是 2026 年全国大学生电子设计竞赛 E 题“拼图装置”的视觉项目。
- 只负责视觉感知、拼图求解、放置后视觉复核和结构化结果输出。
- 不负责机械臂、龙门架、电磁铁、升降机构、运动学、轨迹规划或执行机构控制。
- 本项目从标准 `ros2_ws` 复制而来，后续修改只允许发生在 `26E_vision` 内，不得反向覆盖标准项目。

## 官方题面视觉硬约束

- 题面依据为官方《E题_拼图装置》PDF 的 E-1 至 E-3 页。下列数量、尺寸、时间、摆放、
  光照和验收条件属于不可由本地算法策略放宽的外部约束。
- 为避免把评分项编号误当作软件任务编号，本项目统一使用“基础自备纯色工况”“现场纯白
  工况”“现场牌面工况”三个名称；现场纯白与现场牌面共同属于题面要求第 2 项。

### 场地、启动与运行环境

- 拼图区域为纵向 A4 纸张；碎片由测评人员随机且互不重叠地摆放在上半区，装置将碎片
  移至下半区并拼成目标矩形。视觉算法不得依赖固定初始位置、方向、输入顺序或人工预摆。
- A4 上下区域具有明显实线分界，线宽不大于 `5 mm`。区域定义以校正后的 A4 长边中点
  为准，分界线可作为一致性证据，但不得被误识别为碎片边缘。
- 测评开始前摄像头被遮挡；随机摆放完成后，一键启动与移除遮挡同时发生并开始计时。
  视觉链路不得使用遮挡期间的旧图像，必须在遮挡解除、曝光和场景稳定后获取有效输入。
- 启动后应自动完成拼接，中途不得依赖人工干预。视觉侧应向控制侧提供明确的进行中、
  完成、重试或失败状态；声光结束提示由整机侧实现，不属于本视觉项目的执行职责。
- 装置必须适应正常室内照明，视觉方案不得把特殊补光、固定实验室光照或人工调灯作为
  通过条件；曝光、白平衡、阈值和颜色模型的适用范围必须用现场样本验证。
- `2 min` 是从启动到整机完成的端到端上限，包含感知、求解、通信、移动和必要复核；
  视觉模块不得把自身未计入整机时钟的局部耗时当作题面计时结果。

### 基础自备纯色工况

- 使用自备纵向 A4 和题面图 2 尺寸的固定 4 片碎片。A4 颜色自定；4 片碎片均为纯色、
  颜色一致、厚度不大于 `2 mm`，且碎片上无任何元器件。
- 图 2 对应的完成矩形为 `100 mm × 60 mm`；精确碎片顶点和边长以
  `docs/basic_task_geometry.md` 中对官方图形的数字化记录为准，不得改成任意四片通用拼图。
- 必须在一次启动且不超过 `2 min` 内同时完成：全部碎片由上半区移至下半区；拼成目标
  矩形，碎片位置正确、互不重叠，相邻碎片对应顶点距离均不大于 `20 mm`。

### 现场提供碎片工况

- 继续使用选手自备 A4；现场碎片数量不大于 4，每片边数不大于 5，每条边长不小于
  `20 mm`，且每片至少有一条边位于目标矩形外边界上。
- 现场碎片为约 `0.3 mm` 镀锌薄铁片上粘贴纸片，总厚度不大于 `2 mm`、重量不大于
  `30 g`。视觉验证需覆盖薄片边缘、反光和纸片粘贴造成的实际轮廓误差，不得只用理想
  二值多边形宣称现场适用。
- 目标矩形尺寸范围为 `90 mm × 50 mm` 至 `120 mm × 90 mm`；长短边可在该范围内变化，
  不得固定为基础工况的 `100 mm × 60 mm`。
- 现场纯白工况的碎片表面均为白色；在不超过 `2 min` 内拼成位置正确、互不重叠且相邻
  对应顶点距离不大于 `20 mm` 的目标矩形。
- 现场牌面工况的碎片表面为白底扑克牌牌面；除上述几何条件外，相邻碎片花纹必须对应
  正确。计时越短越好，但最长仍不得超过 `2 min`。

### 视觉验收口径

- 题面几何阈值是相邻碎片对应顶点距离不大于 `20 mm`。项目可在 YAML 中配置更严格的
  内部门限以留出执行误差，但不得把更宽松门限写成题面允许值。
- 完成判定必须基于最终场景重新测量目标矩形、碎片重叠和顶点/纹理对应关系；仅有求解器
  预测或控制侧“动作完成”回执，不能证明满足题面视觉验收条件。若某工况按已确认方案不做
  逐动作复核，也必须保留最终整体验收或明确报告该项尚未验证。
- 纯白工况以外轮廓几何为主；牌面工况必须同时保持完整外轮廓。接缝花纹主要用于
  几何存在非唯一或近似等价解时选择更合理的候选，不要求红桃、黑桃等图案严格像素级
  对齐；内部红黑图案不得被拆成额外碎片。
- 设计报告相关证据至少覆盖碎片识别、拼接算法、测试方案、测试结果完整性和结果分析；
  未经真实样本和整机计时验证的成功率、精度或耗时不得写成已满足题面。

## 相机与标定默认规则

- 摄像头独立固定在工作区顶部，相对于地面和工作平面保持不动，不安装在龙门架或其他运动机构上。
- 龙门架上只允许安装机械控制相关部件；龙门架运动不得改变摄像头位置、姿态或视觉外参。
- 默认硬件条件：固定位置俯视 A4 平面、镜头畸变小于 1%。
- 由于摄像头与地面保持固定，视觉系统应建立长期稳定的工作区绝对坐标系，并向控制侧输出该坐标系中的 X/Y 位置和面内角度。
- 固定工作区绝对坐标系与 A4 局部坐标系必须区分：绝对坐标系不随 A4 纸、龙门架或电磁铁移动；A4 局部坐标系可在启动时根据纸张位置重新计算。
- 默认保存摄像头像素坐标到工作区绝对平面的固定映射；A4 四角检测主要用于确认纸张位置、上下区域和局部坐标，不用于暗示摄像头发生运动。
- 默认不把完整张正友内参和畸变标定作为主流程或题目硬性要求。
- 安装阶段使用固定工作区基准点，或使用处于已知绝对位置的 A4 四角，建立一次性的像素到工作区绝对平面映射；可使用 `findHomography` 或 `getPerspectiveTransform`。
- 运行阶段检测 A4 四角，计算 A4 局部坐标系在工作区绝对坐标系中的位置。A4 移动时只更新 A4 局部变换，不更新固定相机外参和工作区绝对映射。
- 完整内参/畸变标定仅在实测平面误差、边缘误差或透视误差无法满足验收时启用。
- 现场牌面工况例外：运行前必须加载与最终相机模式匹配的实测内参和畸变参数；标定未确认时
  固定返回 `CALIBRATION_REQUIRED`，不得用视场角推导值替代。
- 平面坐标映射必须通过真实 A4 和标定点验证，不能仅凭“畸变小于 1%”宣称精度已达标。

## 默认视觉算法规则

- 基础自备纯色、现场纯白、现场牌面三种工况均以 A4 工作区识别和平面校正作为共用场景入口，不得只为其中一种工况实现。
- 题面没有规定 A4 必须为绿色；绿色或浅绿色 A4 是当前项目的自选实现条件。该选择必须写入配置和测试条件，不得表述为官方硬性要求。
- 采用绿色 A4 时，`green_a4_*` 负责绿色场景/A4 框检测；三种工况默认在 A4 校正图内使用可配置 Canny 边缘提取外轮廓。现场题可叠加动态绿色纸面 Lab 背景差分填充弱边缘内部；关闭 Canny 时，Lab 背景反选或 `white_piece_*` 仅作为可配置回退。HSV、Lab 或 Canny 阈值不得在节点中另设不可调常量。
- 绿色 A4 工作区候选以颜色、闭合凸四边形、面积和边界覆盖率为主，不强制符合 `210:297` 的精确长宽比；只允许使用“排除过度狭长候选”等宽松几何质量门控。
- 拼图视觉链路默认不用 YOLO，也不默认训练目标检测或实例分割模型。
- 默认主链路为：A4 平面映射、颜色/灰度分割、形态学处理、连通域或
  `findContours`、`approxPolyDP`、直线/顶点细化、边长/面积拼图求解、位姿连接与不重叠
  检查、质量门控。
- 由于碎片数量不超过 4 片、边数不超过 5 条且工作背景可控，优先使用可解释的 OpenCV 几何方法。
- 只有在真实样本验证经典分割和轮廓方法不足时，才可提出 YOLO 或 YOLO-seg 作为降级方案或对照实验；启用前必须报告数据集、召回率、误检率、Jetson 实测延迟和为什么几何方案不足。
- YOLO 检测框不得直接替代碎片精确轮廓、顶点或拼接误差测量。

## 迭代收敛后的长期算法决策

以下规则来自拼图总体方案、OpenCV GPU 加速和轮廓算法三组十轮选型。详细比较、淘汰理由和待验证指标统一记录在 `docs/puzzle_piece_scheme_selection.md`；本节只保留会长期影响实现的结论。

### 拼图识别与求解主链

- 三种视觉工况共用纵向 A4 场地：沿 297 mm 长边以 `region_split_ratio=0.5` 划分区域；所有碎片
  初始轮廓必须完整位于上半区，目标拼图轮廓必须完整位于下半区并保留安全边距。任一工况
  初次扫描发现碎片越过上半区时必须拒绝，不得生成运动坐标。
- 基础题固定使用用户确认图示中的 4 块模板，完成图为 `100 mm × 60 mm`：右侧
  `60-80-100 mm` 三角形，以及面积依次为 `480、1080、2040 mm²` 的三个四边形；
  精确顶点和边长以 `docs/basic_task_geometry.md` 为准。基础题模板不匹配时必须拒绝，
  不得退回宽尺寸通用矩形猜测。
- 拼图规划默认在相邻公共接缝的法向上保留可配置的 `0.2 mm` 名义间隙，避免目标轮廓
  重叠；只能平移完整刚体，不得缩放或变形拼图块。该数值是视觉规划值，不等同于未经
  机械落位和最终复核测得的实物间隙。
- 基础题、现场纯白和现场牌面工况在“识别时 A4 内只保留拼图块”的前提下，默认使用
  可配置 Canny 边缘作为分离屏障，并从 A4 背景洪泛填充来保持相近碎片间隙。基础题不得
  再把 Lab 背景差分并回 Canny 掩膜，以免绿色纸面色差桥接独立碎片；现场题可使用 Lab
  绿色背景差分填充大块或弱边缘碎片内部。轮廓质量不合格或题目对应拓扑不匹配时必须
  拒绝，不能回退为猜测轮廓。
- 基础题模板匹配必须区分平面刚体旋转与镜像翻面；仅镜像后才能匹配的碎片输出
  `BASIC_TEMPLATE_FLIP_REQUIRED:P...` 且不生成放置坐标。不得把机械无法通过 X/Y/旋转
  实现的反射变换伪装成已求解。
- 现场提供碎片工况使用 1--4 片、每片最多 5 边且每条边至少 20 mm 的多边形；完成矩形
  长边为 90--120 mm、短边为 50--90 mm，每片至少一条完整边位于完成矩形外边界。
- 现场提供碎片工况的完成矩形方向自由、中心可在 A4 下半区安全边界内选择，不绑定绿色 A4 中心；
  由于所有碎片初始位于上半区，不再采用“保持一片锚定不动”策略，而是在下半区可行
  布局中按总移动和旋转成本确定性选择。
- 现场机械结构可能在 `[move,ok]` 到达时尚未离开 A4 视野；拼图拼装过程不得在
  每次 `[move,ok]` 后重新拍照。协调器应在首次有效识别/求解时缓存本轮全部
  `PuzzlePlacement`，同一时刻只允许一个五字段帧等待 `[move,ok]`，收到确认后
  直接下发队列中的下一条，直到队列完成后再发送 `[map,ok]`。
- 现场牌面工况保持与现场纯白工况一致的逐片闭环；求解主路径必须先用扑克牌白边锁定
  完成矩形外框，白边不得参与内部拼接，最终外露边必须全部为白边。只有白边外框已成立
  且存在旋转、全等片互换等多解时，才用梯度连续性、中心对称性、对角特殊性和非字符角
  纯白色度消歧；不得回退为发挥题一的完整未知边枚举作为牌面主算法。
- 现场提供碎片工况允许下半区内目标中心、方向及全等片排列自由；通过全部几何硬约束的等价解按总移动和
  旋转成本确定性选择，不得因全等片交换导致零分差而沿用基础题的严格唯一解拒绝。
- 所有下发控制侧的拼图旋转差均按顶视图定义：逆时针为正、顺时针为负，范围
  `[-180°, 180°)`；必须使用求解器完整刚体变换，不能用最长边半周方向角替代非对称
  拼图块的实际旋转量。
- 实测拼图算法以“边长/面积两层筛选 + 位姿连接”为固定基础算法：对每个测得碎片记录
  所有边长，先枚举两条由若干边组成的长边链和两条由若干边组成的宽边链，要求两长近似
  相等、两宽近似相等且 `长 × 宽` 与碎片总面积一致；再要求剩余未选边存在正负加减平衡
  为 0 的可能，作为内部接缝可闭合的必要条件；通过筛选后再进入长宽连接、刚体位姿、
  下半区边界和互不重叠检查。
- 当前实测结论是：在现场纯白/挑战一类通用碎片中，只有上述边长/面积算法成功生成
  `SOLVED` 的拼图 plan；虽然计算时间略长，后续优化不得把它替换回原 1 至 4 块子集
  动态规划/二叉合并、接缝纹理软评分或严格唯一解拒绝作为默认算法。旧几何合并、纹理
  相似度、ORB/SIFT 等仅可作为诊断、排序或待验证辅助，不得阻止该算法输出已通过硬门控
  的 plan，除非新的实测证据证明替代方案同样能稳定成功。
- 发挥一位姿连接固定采用锚定式逐片边枚举：固定一块作为刚体基准，递归枚举未放置拼图块
  与当前外露边的全部合法配对，不再按子集分数截断候选。总测量边数默认上限为可配置的
  `18`，超限明确拒绝；先穷举完整边，找不到矩形时再穷举部分边/T 形接缝。边长/面积
  必要条件、互不重叠和矩形闭合仍为硬门控。四片拼图搜索使用固定 `5` 个 C++ 工作线程，
  仅并行根候选分支并按分支编号确定性汇总，不在递归层级继续创建线程。
- 唯一首选链路为：固定相机采集与清晰度/稳定性筛选 -> 绿色 HSV 场景掩膜与四角平面校正 -> 独立对象分割 -> 形态学处理 -> 精确轮廓与顶点细化 -> 边长/面积两层筛选 -> 长宽连接、刚体位姿和互不重叠检查 -> 质量门控。基础自备纯色、现场纯白和现场牌面工况均在首次有效求解后缓存整套放置队列，避免机械结构遮挡时重复采图；现场牌面工况在同一几何计划上追加接缝花纹评分，花纹评分只排序几何可行候选。
- 现场牌面工况默认使用 Canny 外边缘候选和 `RETR_EXTERNAL` 轮廓，避免扑克牌内部黑色、
  红色花纹被当作额外碎片；Lab 背景反选仅作为关闭 Canny 时的回退。为发现违规初始摆放，
  感知可检查 A4 上下两区，但同一任务代只在首次有效扫描时要求全部碎片完整位于上半区。
- A4 平面校正后统一使用毫米坐标；未通过实测验证的工作区绝对映射不得向控制侧输出绝对坐标。
- 拼图求解以边长链、总面积、剩余边正负平衡、位姿连接、重叠、目标矩形尺寸和闭合性
  作为硬约束；ZNCC 或 SSIM 只作为接缝纹理软评分，ORB 加 RANSAC 仅作为纹理充分时的
  辅助证据，不得作为现场纯白/挑战一类通用碎片的默认通过条件，也不得要求牌面花纹
  严格对齐后才允许输出。
- 现场牌面工况可利用题目限制做算法特化：当拼图块边界内侧存在紧挨边界的明显花纹边时，
  优先比较这些信息量高的边及其对应碎片；该优先级只能影响候选排序和搜索效率，不能跳过
  几何硬约束，也不能排除没有明显边缘花纹但几何可行的候选。
- 轮廓不完整、尺寸越界、存在明显重叠/孔洞或映射无效时，必须输出 `RETRY` 或 `FAIL`，
  不得发送猜测位姿。最优与次优解分差门槛只有在具体任务 YAML 显式配置为正数时才作为
  拒绝条件；现场牌面工况默认由花纹软评分在几何可行候选中择优。
- 不使用端到端网络直接回归拼图布局；YOLO-seg 或 U-Net 只有在真实样本证明经典前景分割不足后，才可作为 ROI/掩膜降级对照，最终量测仍由几何轮廓层完成。

### OpenCV GPU/CPU 分工

- 默认架构为“GPU 大矩阵前处理 + CPU 轮廓/几何/求解”，不追求整条链路 CUDA 化。
- GPU 路径必须让单帧 `GpuMat` 常驻：最多上传一次，连续执行透视变换、HSV 转换、独立对象 `inRange`、颜色排除和 opening/closing，最终只下载一次二值 mask；禁止为每个算子反复上传、下载或同步。
- `findContours`、轮廓排序、边线拟合、角点细化、接缝组合、子集动态规划和质量门控以 CPU/OpenCV 结果为基准。
- OpenCV CUDA 必须运行时探测，且保留结果语义一致的 CPU 回退；CUDA 不可用、初始化失败、结果不一致或端到端 P50/P95 没有实际收益时，使用 CPU 路径。
- GPU 是否成为默认路径，只能依据目标 Jetson 上包含上传、算子、下载和 CPU 后处理在内的端到端实测决定；不得只引用 kernel 时间或桌面端结果。

### 精确轮廓算法

- 连通域分析只用于碎片计数、面积和外接框粗筛；最终有序轮廓使用 `findContours(RETR_EXTERNAL, CHAIN_APPROX_NONE)`。
- `approxPolyDP` 只生成 3 至 5 边多边形初值，应在 YAML 配置的 epsilon 候选范围内比较；不得把单次近似结果直接当作毫米级最终顶点。
- 按初始顶点将完整轮廓点分配到各边，使用稳健 `fitLine` 或加权最小二乘拟合边线，并用相邻边线交点更新顶点；局部梯度、连续性和拟合残差合格时，才可使用 `cornerSubPix` 进一步细化。
- `minAreaRect` 只提供粗方向；`convexHull` 不得用于修正最终拼图轮廓；Canny/HoughLinesP
  只生成边缘或断裂边候选，必须经过外轮廓、面积、边数、边长和拟合残差门控，不得单独
  作为实体最终边界。
- 默认使用 `RETR_EXTERNAL` 排除内部扑克纹理轮廓；异常孔洞应成为质量门控信号。明确粘连时可试验 Watershed，但不得静默拆分后直接输出。
- 候选碎片不得按位置排序后直接截断为前 4 个；必须先按面积、边数、最小边长、轮廓残差、区域约束和跨帧稳定性综合筛选，数量仍不可信时拒绝求解。
- 轮廓算法验收至少报告人工标注轮廓 IoU、顶点毫米误差 P50/P95、边线拟合 RMS/最大残差、计数准确率、粘连误分率、孔洞/纹理误检率及 Jetson 轮廓阶段 P50/P95 延迟。

## 电磁铁视觉标志边界

- 电磁铁顶部特定颜色可用于检测末端执行器在 A4 平面内的 X/Y 位置和放置后相对误差。
- 颜色检测必须结合形状、面积、位置范围和连续帧稳定性，避免与碎片颜色混淆。
- 顶视相机不能仅凭颜色判断电磁铁升降高度；高度和执行器动作由机械/控制侧负责。
- 龙门架及电磁铁属于运动遮挡物；碎片识别、拼图求解和放置复核应在控制侧确认运动停止且电磁铁离开关键轮廓后采集有效帧。
- 不得把龙门架运动解释为相机运动，也不需要为龙门架位置变化重新估计相机外参。

## 串口与 HMI 默认规则

- 当前 26E 硬件实测 UART1(`/dev/ttyTHS1`) 本地回环失败；为保证与 MCU 通信，
  默认 MCU 使用 UART0(`/dev/ttyTHS3`)。
- 26E 串口角色仍通过 `mcu_port`/`hmi_port` 分配；串口屏直连 Jetson 时默认
  使用 9600bps 8N1。若串口屏必须直连当前 Jetson，需先修复或替换 UART1。
- 单片机与 Jetson 正常连接，默认波特率为 115200bps；视觉下发的五字段帧和
  `[move,ok]` 确认均走 `serial_bridge_node`，不得由其他节点直接打开 UART。
- 当前五字段帧 `[x0,y0,x1,y1,angle]` 中的 X/Y 使用绿框归一化坐标：绿框左下角为
  `(0,0)`，右下角为 `(1,0)`，左上角为 `(0,1)`，中心为 `(0.5,0.5)`；X 向右，
  Y 向上。全部碎片复核完成后，视觉侧向 MCU 发送 `[map,ok]`。
- 当前配置为 MCU 使用 UART0、串口屏直连 UART1；如现场再次调整 UART0/UART1，只修改
  `mcu_port`/`hmi_port` 配置，不改变协议语义；波特率按角色分别使用
  `baud_rate=115200` 与 `hmi_baud_rate=9600`。
- 串口屏也可能接在单片机侧，由单片机转发 HMI 与 Jetson 双向数据；该工况设置
  `hmi_over_mcu=true`，避免 Jetson 同时把同一路 UART 打开成 MCU 与 HMI 两个端口。
- 后续 debug/work 默认启用 `serial_bridge_node` 与 `hmi_control_node`。除非用户明确要求
  关闭串口，启动项目时不再因为串口拓扑未确认而禁用串口节点。
- 串口屏修改的全部参数必须永久写入 `vision_system.yaml` 中对应的 `hmi_value_*`
  YAML 键；写入成功后才回 ACK。已有运行期订阅者的参数可实时生效；debug 模式开关等
  launch 级参数写入 YAML 后，在默认 `mode:=auto` 的下一次启动生效。

## 质量与验证

- 不得把未实测的精度、成功率、FPS 或延迟写成已确认结论。
- 每次输出前检查 A4 定位、碎片数量、轮廓残差、矩形闭合、重叠面积、顶点间距和解的唯一性。
- 最终视觉验收使用题面 `20 mm` 顶点距离上限，并对牌面工况增加接缝花纹对应检查；算法内部更严格的安全门限必须配置化并记录。
- 重试和恢复必须自动进行并计入整机 `2 min` 时限；不得通过请求人工重新摆放、调光或确认来完成题面流程。
- 视觉结果不可信时输出拒绝或重试状态，不向控制侧发送猜测位姿。
- 本项目禁止使用 WSL 进行构建、运行或测试；ROS2 与算法运行验证直接在 Jetson ARM64 实机完成，Windows 仅做源码与静态检查。

## 更新与回滚

- 每次修改前先备份所有将被修改或覆盖的现有文件；新增文件记录在变更清单中，回滚时删除对应新增文件。
- Windows 备份放入 `Copyfiles/backups/26E_vision/<时间戳>/`，记录原相对路径、修改前 SHA256、变更说明和恢复命令。
- 向 Jetson 部署更新前，先备份 Jetson 上对应的上一版文件或项目快照，再从 Windows 权威副本选择性覆盖。
- 构建目录、安装目录、日志和临时测试文件不能作为回滚依据。
- 每次交付必须给出本次备份位置、验证结果及回滚方法，确保不依赖记忆即可恢复到更新前状态。
